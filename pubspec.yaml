name: mypage_setting
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/Image/Logo_L.png  # 이미지 경로 추가
    - assets/Image/HomeIcon.png
    - assets/Image/RankIcon.png
    - assets/Image/MypageIcon.png
